{
  "root": true,
  "extends": ["next/core-web-vitals"],
  "rules": {
    // Beispiel: Warnungen statt Fehler bei unbenutzten Variablen (optional anpassen)
    "@typescript-eslint/no-unused-vars": ["warn", { "argsIgnorePattern": "^_" }]
  }
}
